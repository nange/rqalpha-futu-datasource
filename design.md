# 设计思路

本项目是基于富途OpenAPI接口，实现RQAlpha框架自定义数据源，用于支持A股、港股、美股的股票回测。
因此需要实现RQAlpha框架提供的`AbstractDataSource`接口，需要实现的方法我已经在`datasource.py`中进行了空实现。
你需要根据富途OpenAPI接口文档以及RQAlpha框架的文档，实现这些方法。

注意：

1. 富途OpenAPI是数据的提供者，可以把富途提供的原始数据作为csv文件等通用格式下载到本地某个指定目录（可以作为单独的脚本执行，下载到指定目录，注意区分1分钟K线，3分钟K线，5分钟K线，日K线，周K线，月K线数据，可以指定周期，也可以下载全部周期数据，默认下载全部周期数据，只支持前复权数据；支持指定股票代码或者股票列表）
2. `datasource.py`中的实现方法，是需要从上面指定的目录读取数据，进行处理后（注意富途和RQAlpha框架的数据格式不同，如股票命名方式不同），返回RQAlpha框架需要的数据格式
3. 只需要关心和实现股票回测相关的函数，其他函数可以空实现
